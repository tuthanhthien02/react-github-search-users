import React from "react";
import { Info, Search, User, Followers } from "../components";

const Dashboard = () => {
    return (
        <main>
            <Search />
            <Info />
        </main>
    );
};

export default Dashboard;
